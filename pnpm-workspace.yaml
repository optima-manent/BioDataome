overrides:
  postcss: 8.5.19
  sharp: 0.35.0

# Native build hooks required by the pinned web toolchain. Keeping this list
# explicit makes clean installs fail closed for any newly introduced script.
allowBuilds:
  esbuild: true
  sharp: true
  unrs-resolver: true
  workerd: true
