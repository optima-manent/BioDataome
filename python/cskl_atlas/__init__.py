"""C-SKL Atlas production orchestration layer.

The vendored :mod:`cskl` module remains the numerical source of truth. This
package adds immutable artifact contracts, incremental orchestration, query
serving, provenance, and recoverable job state around that kernel.
"""

__version__ = "0.4.0"
